import classes from "./DashboardPage.module.css";
const DashboardPage = () => {
  return (
    <div className={`page ${classes.dashboard}`}>
      <h2>Resultados</h2>
    </div>
  );
};

export default DashboardPage;
